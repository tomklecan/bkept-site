<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bkept | Master Data Protocol</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    <style>
        /* --- BKEPT IDENTITY CORE --- */
        :root {
            --bk-gradient: linear-gradient(110deg, #8E7338 0%, #D6B569 35%, #F9E7BA 50%, #D6B569 65%, #8E7338 100%);
            --bk-gold-solid: #C5A059;
            --bk-dark: #2D3436;
            --bk-void: #1A1A1A;
            --bk-surface: #F9FAFB;
            --bk-border: #E2E8F0;
            --bk-alert: #FFF8E1;
        }

        * { box-sizing: border-box; outline: none; }
        
        body { 
            background-color: #525659; 
            font-family: 'Inter', sans-serif; 
            color: var(--bk-dark); 
            margin: 0; 
            padding: 40px 20px;
            display: flex; 
            justify-content: center;
        }

        .doc-container {
            background: #FFFFFF;
            width: 210mm; /* A4 Width */
            min-height: 297mm;
            padding: 15mm 20mm;
            box-shadow: 0 20px 60px rgba(0,0,0,0.25);
            display: flex;
            flex-direction: column;
        }

        /* --- TYPOGRAPHY --- */
        .bk-logo {
            font-size: 24px; font-weight: 800; letter-spacing: 2px;
            background: var(--bk-gradient); -webkit-background-clip: text; 
            -webkit-text-fill-color: transparent; text-transform: lowercase; display: block;
        }

        h1 { 
            font-size: 28px; font-weight: 800; text-transform: uppercase; 
            margin: 10px 0 5px 0; letter-spacing: -1px; color: var(--bk-void);
        }

        .sub-header { 
            font-size: 11px; font-weight: 700; text-transform: uppercase; 
            letter-spacing: 2px; color: #B2BEC3; margin-bottom: 30px; 
            border-bottom: 2px solid var(--bk-gold-solid); padding-bottom: 20px;
        }

        /* --- SECTIONS --- */
        .section-block { margin-bottom: 40px; page-break-inside: avoid; }
        
        .section-title {
            background: var(--bk-void); color: white;
            padding: 8px 15px; font-size: 12px; font-weight: 800;
            text-transform: uppercase; letter-spacing: 1px;
            margin-bottom: 15px; display: inline-block; border-radius: 2px;
        }

        /* --- INPUTS --- */
        .input-row { display: grid; grid-template-columns: 1fr 1fr; gap: 20px; margin-bottom: 15px; }
        .input-full { width: 100%; margin-bottom: 15px; }

        .label { 
            font-size: 9px; font-weight: 700; text-transform: uppercase; 
            color: #A0AEC0; margin-bottom: 4px; display: block; 
        }

        .editable {
            background: var(--bk-alert);
            border-bottom: 1px dashed var(--bk-gold-solid);
            padding: 8px; font-size: 13px; font-weight: 600;
            color: var(--bk-dark); min-height: 35px;
            display: flex; align-items: center;
        }
        .editable:empty:before { content: attr(placeholder); color: #B2BEC3; font-weight: 400; }
        .editable:focus { background: white; border-bottom: 2px solid var(--bk-gold-solid); }

        /* --- TABLES --- */
        table { width: 100%; border-collapse: collapse; margin-bottom: 10px; }
        th { 
            text-align: left; font-size: 9px; text-transform: uppercase; 
            color: var(--bk-gold-solid); border-bottom: 2px solid var(--bk-void); 
            padding: 8px 5px; letter-spacing: 0.5px;
        }
        td { border-bottom: 1px solid var(--bk-border); padding: 5px; vertical-align: top; }
        
        .add-btn {
            background: #F1F2F6; color: var(--bk-dark); border: none;
            padding: 6px 12px; font-size: 10px; font-weight: 700; 
            text-transform: uppercase; cursor: pointer; border-radius: 2px;
        }
        .add-btn:hover { background: var(--bk-gold-solid); color: white; }

        /* --- UPLOAD CHECKPOINT --- */
        .upload-checkpoint {
            border: 1px solid var(--bk-gold-solid);
            background: #FFFCF5;
            padding: 15px; display: flex; align-items: center; gap: 15px;
            margin-top: 15px; border-radius: 4px;
        }
        .upload-icon { 
            font-size: 20px; color: var(--bk-gold-solid); 
        }
        .upload-text { font-size: 11px; color: var(--bk-dark); font-weight: 600; }
        .upload-tag { 
            background: var(--bk-void); color: white; padding: 2px 6px; 
            font-size: 9px; text-transform: uppercase; margin-right: 5px; 
        }

        /* --- PRINT CONTROLS --- */
        .controls {
            position: fixed; top: 20px; right: 20px; display: flex; gap: 10px;
        }
        .ctl-btn {
            background: var(--bk-gradient); color: var(--bk-void);
            padding: 12px 24px; border: none; font-weight: 800;
            text-transform: uppercase; cursor: pointer; border-radius: 4px;
            box-shadow: 0 4px 10px rgba(0,0,0,0.3); font-size: 12px;
        }

        @media print {
            body { background: white; padding: 0; }
            .doc-container { box-shadow: none; margin: 0; width: 100%; }
            .controls, .add-btn { display: none !important; }
            .section-block { break-inside: avoid; }
            .editable:empty:before { content: ""; } /* Hide placeholders on print */
        }
    </style>
</head>
<body>

<div class="controls">
    <button class="ctl-btn" onclick="window.print()">Save as PDF</button>
</div>

<div class="doc-container">
    
    <span class="bk-logo">bkept.</span>
    <h1>Master Data Protocol</h1>
    <div class="sub-header">Client Intake Packet // Confidential</div>

    <div class="section-block">
        <div class="section-title">01 // Entity Information</div>
        <div class="input-full">
            <span class="label">Registered Legal Name</span>
            <div class="editable" contenteditable="true" placeholder="Ex: Acme Holdings LLC"></div>
        </div>
        <div class="input-row">
            <div>
                <span class="label">DBA / Trade Name</span>
                <div class="editable" contenteditable="true" placeholder="If applicable..."></div>
            </div>
            <div>
                <span class="label">Entity Type</span>
                <div class="editable" contenteditable="true" placeholder="LLC, S-Corp, C-Corp..."></div>
            </div>
        </div>
        <div class="input-row">
            <div>
                <span class="label">Federal EIN (Tax ID)</span>
                <div class="editable" contenteditable="true" placeholder="XX-XXXXXXX"></div>
            </div>
            <div>
                <span class="label">State of Incorporation</span>
                <div class="editable" contenteditable="true" placeholder="Ex: Delaware, New Jersey..."></div>
            </div>
        </div>
        
        <div class="upload-checkpoint">
            <div class="upload-icon">&#8679;</div>
            <div class="upload-text">
                <span class="upload-tag">Upload</span> Articles of Incorporation / Formation Documents
            </div>
        </div>
    </div>

    <div class="section-block">
        <div class="section-title">02 // Bank Accounts (Depository)</div>
        <table id="bankTable">
            <thead>
                <tr>
                    <th width="30%">Bank Name</th>
                    <th width="30%">Account Nickname</th>
                    <th width="15%">Last 4 Digits</th>
                    <th width="25%">Purpose</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td><div class="editable" contenteditable="true"></div></td>
                    <td><div class="editable" contenteditable="true"></div></td>
                    <td><div class="editable" contenteditable="true"></div></td>
                    <td><div class="editable" contenteditable="true" placeholder="Ops / Tax / Payroll"></div></td>
                </tr>
            </tbody>
        </table>
        <button class="add-btn" onclick="addRow('bankTable')">+ Add Row</button>

        <div class="upload-checkpoint">
            <div class="upload-icon">&#8679;</div>
            <div class="upload-text">
                <span class="upload-tag">Upload</span> Past 3 Months of Bank Statements (PDF)
            </div>
        </div>
    </div>

    <div class="section-block">
        <div class="section-title">03 // Credit Cards (Liability)</div>
        <table id="ccTable">
            <thead>
                <tr>
                    <th width="30%">Institution</th>
                    <th width="30%">Card Nickname</th>
                    <th width="15%">Last 4 Digits</th>
                    <th width="25%">Primary User</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td><div class="editable" contenteditable="true"></div></td>
                    <td><div class="editable" contenteditable="true"></div></td>
                    <td><div class="editable" contenteditable="true"></div></td>
                    <td><div class="editable" contenteditable="true"></div></td>
                </tr>
            </tbody>
        </table>
        <button class="add-btn" onclick="addRow('ccTable')">+ Add Row</button>

        <div class="upload-checkpoint">
            <div class="upload-icon">&#8679;</div>
            <div class="upload-text">
                <span class="upload-tag">Upload</span> Past 3 Months of Credit Card Statements (PDF)
            </div>
        </div>
    </div>

    <div class="section-block">
        <div class="section-title">04 // Income Platforms (Processors)</div>
        <table id="incomeTable">
            <thead>
                <tr>
                    <th width="40%">Platform</th>
                    <th width="60%">Linked Bank Account (Deposits to)</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td><div class="editable" contenteditable="true" placeholder="Stripe, Square, PayPal, Shopify..."></div></td>
                    <td><div class="editable" contenteditable="true"></div></td>
                </tr>
            </tbody>
        </table>
        <button class="add-btn" onclick="addRow('incomeTable')">+ Add Row</button>
        
        <div class="upload-checkpoint">
            <div class="upload-icon">&#9881;</div>
            <div class="upload-text">
                <span class="upload-tag">Action</span> Invite <strong>tomklecan@bkept.co</strong> as an Admin/Accountant user to these platforms.
            </div>
        </div>
    </div>

    <div class="section-block">
        <div class="section-title">05 // Liabilities (Loans / LOCs)</div>
        <table id="loanTable">
            <thead>
                <tr>
                    <th width="30%">Lender Name</th>
                    <th width="20%">Total Balance</th>
                    <th width="20%">Monthly Pmt</th>
                    <th width="30%">Notes</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td><div class="editable" contenteditable="true" placeholder="SBA, Auto Loan..."></div></td>
                    <td><div class="editable" contenteditable="true"></div></td>
                    <td><div class="editable" contenteditable="true"></div></td>
                    <td><div class="editable" contenteditable="true"></div></td>
                </tr>
            </tbody>
        </table>
        <button class="add-btn" onclick="addRow('loanTable')">+ Add Row</button>

        <div class="upload-checkpoint">
            <div class="upload-icon">&#8679;</div>
            <div class="upload-text">
                <span class="upload-tag">Upload</span> Loan Agreements & Amortization Schedules
            </div>
        </div>
    </div>

    <div class="section-block">
        <div class="section-title">06 // Payroll (Human Capital)</div>
        <div class="input-row">
            <div>
                <span class="label">Payroll Provider</span>
                <div class="editable" contenteditable="true" placeholder="Gusto, ADP, Paychex..."></div>
            </div>
            <div>
                <span class="label">Pay Schedule</span>
                <div class="editable" contenteditable="true" placeholder="Bi-Weekly, 15th/30th..."></div>
            </div>
        </div>
        <div class="upload-checkpoint">
            <div class="upload-icon">&#9881;</div>
            <div class="upload-text">
                <span class="upload-tag">Action</span> Grant "Accountant/Admin" access to <strong>tomklecan@bkept.co</strong>.
            </div>
        </div>
    </div>

    <div class="section-block">
        <div class="section-title">07 // Sales Tax (Nexus)</div>
        <div class="input-row">
            <div>
                <span class="label">Filing State(s)</span>
                <div class="editable" contenteditable="true" placeholder="List all states..."></div>
            </div>
            <div>
                <span class="label">Filing Frequency</span>
                <div class="editable" contenteditable="true" placeholder="Monthly, Quarterly, Annual..."></div>
            </div>
        </div>
        <div class="input-full">
            <span class="label">State Tax Portal Login (Or ID)</span>
            <div class="editable" contenteditable="true" placeholder="Provide URL or ID..."></div>
        </div>
        <div class="upload-checkpoint">
            <div class="upload-icon">&#8679;</div>
            <div class="upload-text">
                <span class="upload-tag">Upload</span> Most Recent Sales Tax Filing
            </div>
        </div>
    </div>

    <div class="section-block">
        <div class="section-title">08 // Tax Strategy</div>
        <div class="input-full">
            <span class="label">CPA Firm / Preparer Name & Email</span>
            <div class="editable" contenteditable="true"></div>
        </div>
        <div class="input-row">
            <div>
                <span class="label">Last Filed Year</span>
                <div class="editable" contenteditable="true" placeholder="2024"></div>
            </div>
            <div>
                <span class="label">Fiscal Year End</span>
                <div class="editable" contenteditable="true" placeholder="December 31st"></div>
            </div>
        </div>
        <div class="upload-checkpoint">
            <div class="upload-icon">&#8679;</div>
            <div class="upload-text">
                <span class="upload-tag">Upload</span> Most Recent Federal & State Tax Return
            </div>
        </div>
    </div>

    <div style="text-align: center; margin-top: 50px; font-size: 11px; color: #B2BEC3; text-transform: uppercase;">
        Bkept Performance Bookkeeping | Confidential Data Protocol
    </div>

</div>

<script>
    function addRow(tableId) {
        var table = document.getElementById(tableId).getElementsByTagName('tbody')[0];
        var newRow = table.rows[0].cloneNode(true);
        var cells = newRow.getElementsByTagName('div');
        for(var i=0; i<cells.length; i++) {
            cells[i].innerText = "";
        }
        table.appendChild(newRow);
    }
</script>

</body>
</html>
